Spatial Node Flow is what I call my algorithm. It isn't the most efficient but it is pretty cool to see it work and make the maze. 

This utilizes multiple queue's (Tables) to do everything. It doesn't always treat them as a queue of course but its the thought that counts B)
